(function(doc){var storeFilters=()=>{let formData=new FormData(doc.getElementById('event-filter'));let data={};for(const[key,value] of formData.entries())data[key]=value;localStorage.setItem('filter',JSON.stringify(data))},init=JSON.parse(localStorage.getItem('filter')||'{}');for(const input of doc.querySelectorAll('input[form="event-filter"]')){(input.getAttribute('type')==='checkbox'&&input.getAttribute('name') in init)&&(input.checked=!0);input.addEventListener('change',storeFilters)}})(document);
